let body = document.querySelector("body");
let w = window.innerWidth;
let h = window.innerHeight;
let canvas = `<canvas style='opacity:.2' width='${w}' height='${h}'></canvas>`;
body.innerHtml = canvas;
